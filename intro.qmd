# Introducción

En ArchLinux no es necesario instalar Python, pues este ya viene con la distribución. Sin embargo, para poder crear documentos con Quarto o utilizar Jupyter es necesario instalar este pero no de la manera recomendada para otros sistemas operativos. Debemos hacerlo mediante el gestor Pacman.

```bash
sudo pacman -S jupyterlab
```
